description = "Add a capability or MCP tool"
prompt = """
Follow the playbook in docs/contributing/add-a-tool.md, obeying the rules in AGENTS.md.

Add: {{args}}

Finish with `npm test` and `npm run lint` green, and include a test that fails
without the change.
"""

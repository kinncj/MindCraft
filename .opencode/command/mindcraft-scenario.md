---
description: Add a prebuilt world
---

Follow the playbook in @docs/contributing/add-a-scenario.md, obeying the rules in @AGENTS.md.

Add: $ARGUMENTS

Finish with `npm test` and `npm run lint` green, and include a test that fails
without the change.
